---
export interface Props { avis: any[] }
import { Card, CardContent, CardTitle, CardDescription } from './starwind/card';
const { avis = [] } = Astro.props;
---

{avis.length > 0 ? (
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
    {avis.map((a, index) => (
      <Card variant="luxury" withHover={true} withAnimation={true} class={`animate-stagger-${(index % 6) + 1}`}>
        <CardContent variant="luxury" class="p-6">
          <div class="flex items-start gap-4 mb-4">
            <div class="w-12 h-12 bg-gradient-to-br from-brand-gold-primary to-brand-rose-primary rounded-full flex items-center justify-center text-brand-ivory font-heading text-lg flex-shrink-0">
              {(a.utilisateur || a.nom || 'U').charAt(0).toUpperCase()}
            </div>
            <div class="flex-1">
              <CardTitle variant="luxury" size="sm" class="font-accent text-brand-gold-primary uppercase tracking-wider text-sm mb-1">
                {a.utilisateur || a.nom || 'Utilisateur anonyme'}
              </CardTitle>
              <div class="flex items-center gap-1 mb-2">
                {[...Array(5)].map((_, i) => (
                  <span class={`text-lg ${i < (a.note || 0) ? 'text-brand-gold-primary' : 'text-brand-taupe'}`}>
                    ⭐
                  </span>
                ))}
                <span class="ml-2 text-sm font-body text-brand-taupe">
                  {a.note || 0}/5
                </span>
              </div>
            </div>
          </div>
          <blockquote class="font-body text-brand-charcoal leading-relaxed italic">
            "{a.commentaire || 'Aucun commentaire'}"
          </blockquote>
          <div class="mt-4 pt-4 border-t border-brand-champagne">
            <div class="text-xs font-accent text-brand-taupe uppercase tracking-wider">
              Avis vérifié • Formation
            </div>
          </div>
        </CardContent>
      </Card>
    ))}
  </div>
) : (
  <Card variant="luxury" class="text-center max-w-md mx-auto">
    <CardContent variant="luxury" class="p-8">
      <div class="w-16 h-16 bg-gradient-to-br from-brand-gold-primary to-brand-rose-primary rounded-full flex items-center justify-center mx-auto mb-4">
        <span class="text-2xl">🎓</span>
      </div>
      <CardTitle variant="luxury" size="md" class="mb-2">Aucun avis pour le moment</CardTitle>
      <CardDescription variant="luxury">
        Soyez le premier à partager votre expérience avec cette formation.
      </CardDescription>
    </CardContent>
  </Card>
)}
